#include <stdio.h>

int main(void)
{
    int value = 13%2;
    // printf("The remainder of the equation 13/2 is equal to: %i ",value);

    // printing an int variable
    int i1 = 1;
    int i2 = 2;
    int i3 = 3;
    char LetA = 'A';
    float ie = 12.2;
    printf("i1 = %i, i2 = %i, Let A is: %c", i1, i2, LetA);
    //Notes:
    //  The order of placeholders and variables written after the
    //  is what determines what will be where
    
}

